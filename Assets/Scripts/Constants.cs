

public class Constants
{
    public const int TICKS_PER_SEC = 60;
    public const int MS_PER_TICK = 1000 / TICKS_PER_SEC;
    public const float MS_PER_SECOND = MS_PER_TICK / 1000f;
    public const int BUFFER_SIZE = 256;
}
